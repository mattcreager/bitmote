<label>host</label>
{{#if isHost}}
    <input type="text" id="meeting-host" placeholder="Darth Vader <dvader@gmail.com>" value=""> 
    <span class="is-host">You're The Host</span>
{{else}}
    <span id="meeting-host"></span>
    <span class="is-host">You're A Guest</span>
{{/if}}